# Artifacts Agent Guide

Artifacts are generated outputs from a task session.

Examples:

- PR title and body
- work item update draft
- QA handoff
- release note

Rules:

- drafts are safe to generate
- posting or creating external artifacts requires approval
- artifacts should cite session evidence such as changed files and test results
- do not include secrets or raw sensitive content

