{
  "name": "openpome",
  "version": "0.2.0",
  "private": true,
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.4",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "pnpm -r --if-present lint",
    "test": "tsc -b",
    "pome": "pnpm --filter @openpome/cli start",
    "validate": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
